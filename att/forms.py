from django import forms  
from django.utils import timezone
from .models import time_slots
"""class AttendanceMarkForm(forms.Form):
    date    = forms.DateField(default = timezone.now)
    period  = forms.CharField(choices = time_slots, max_length= 10)

"""